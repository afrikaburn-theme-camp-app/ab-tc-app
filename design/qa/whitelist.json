{
  "mhKYh": "Directory mobile filter-chip row: horizontally scrollable by design (clip:true); chips overflow right edge deliberately.",
  "dwrhE": "Header Brand: AB emblem mark sits snug against the logo banner image; the 17px box overlap lands in the image's transparent padding — verified clean in renders.",
  "C1y2Qr": "Header Brand (teal AppShell): same emblem/logo snug composition as dwrhE.",
  "QxyA1": "2FA QR code container: #FFFFFF is correct — QR codes require a true-white quiet zone regardless of theme.",
  "nGSGC": "Supplier Onboarding mobile header notification bell: Unread Badge sits over the bell glyph's top-right corner (absolute x=17,y=-1 per notifications-spec) — the intentional notification-count overlay, not a layout collision.",
  "hg1t1": "Supplier Standing mobile header notification bell: Unread Badge overlays the bell top-right corner (same spec'd notification-count overlay as nGSGC).",
  "R30biI": "Library-only payment badge/PaymentDetailsBlock components: reserved for future logistics apps (payment-REFERENCE tracking is allowed there per synthesis); used on no page frame — the audit itself proves pages are payment-free.",
  "y0NJ4": "Library-only payment badge/PaymentDetailsBlock components: reserved for future logistics apps (payment-REFERENCE tracking is allowed there per synthesis); used on no page frame — the audit itself proves pages are payment-free.",
  "eUNj3": "Library-only payment badge/PaymentDetailsBlock components: reserved for future logistics apps (payment-REFERENCE tracking is allowed there per synthesis); used on no page frame — the audit itself proves pages are payment-free.",
  "K4Sn5": "Library-only payment badge/PaymentDetailsBlock components: reserved for future logistics apps (payment-REFERENCE tracking is allowed there per synthesis); used on no page frame — the audit itself proves pages are payment-free.",
  "rUgzL": "Library-only payment badge/PaymentDetailsBlock components: reserved for future logistics apps (payment-REFERENCE tracking is allowed there per synthesis); used on no page frame — the audit itself proves pages are payment-free.",
  "SNK0n": "Library-only payment badge/PaymentDetailsBlock components: reserved for future logistics apps (payment-REFERENCE tracking is allowed there per synthesis); used on no page frame — the audit itself proves pages are payment-free.",
  "jRusG": "Library-only payment badge/PaymentDetailsBlock components: reserved for future logistics apps (payment-REFERENCE tracking is allowed there per synthesis); used on no page frame — the audit itself proves pages are payment-free.",
  "oQjM8": "AppShell bell unread badge: absolute-positioned on the bell's corner by design (standard notification pattern).",
  "FOCTm": "AppShell (apricot) bell unread badge: same intentional corner overlap as oQjM8.",
  "POryy": "Supplier onboarding copy: describes the real AB Supplier Depot deposit procedure (paid to AfrikaBurn off-platform, status tracked here) — legitimate payment mention per docs/technical-spec/12-suppliers.md guardrails.",
  "b4q0A": "Supplier fee copy: reworded 25 Jul to state receipt-confirmation only; mention of payment is the lawful tracked-only kind.",
  "X1own": "Role color swatch / role chip: renders a USER-CHOSEN role color (data, not theme) — raw hex is correct here per Roles v2 spec (roles have color + emoji).",
  "kPC7T": "Role color swatch / role chip: renders a USER-CHOSEN role color (data, not theme) — raw hex is correct here per Roles v2 spec (roles have color + emoji).",
  "j5tmRH": "Role color swatch / role chip: renders a USER-CHOSEN role color (data, not theme) — raw hex is correct here per Roles v2 spec (roles have color + emoji).",
  "gDrX9": "Role color swatch / role chip: renders a USER-CHOSEN role color (data, not theme) — raw hex is correct here per Roles v2 spec (roles have color + emoji).",
  "CWAy0": "Role color swatch / role chip: renders a USER-CHOSEN role color (data, not theme) — raw hex is correct here per Roles v2 spec (roles have color + emoji).",
  "ZDC0h": "Role color swatch / role chip: renders a USER-CHOSEN role color (data, not theme) — raw hex is correct here per Roles v2 spec (roles have color + emoji).",
  "cFmyd": "Role color swatch / role chip: renders a USER-CHOSEN role color (data, not theme) — raw hex is correct here per Roles v2 spec (roles have color + emoji).",
  "W9OPJZ": "Role color swatch / role chip: renders a USER-CHOSEN role color (data, not theme) — raw hex is correct here per Roles v2 spec (roles have color + emoji).",
  "XwoA9": "Role color swatch / role chip: renders a USER-CHOSEN role color (data, not theme) — raw hex is correct here per Roles v2 spec (roles have color + emoji).",
  "EdjIq": "Role color swatch / role chip: renders a USER-CHOSEN role color (data, not theme) — raw hex is correct here per Roles v2 spec (roles have color + emoji).",
  "CyY4B": "Role color swatch / role chip: renders a USER-CHOSEN role color (data, not theme) — raw hex is correct here per Roles v2 spec (roles have color + emoji).",
  "jt8NA": "Role color swatch / role chip: renders a USER-CHOSEN role color (data, not theme) — raw hex is correct here per Roles v2 spec (roles have color + emoji).",
  "jjy0g": "Role color swatch / role chip: renders a USER-CHOSEN role color (data, not theme) — raw hex is correct here per Roles v2 spec (roles have color + emoji).",
  "y8R4EP": "Role color swatch / role chip: renders a USER-CHOSEN role color (data, not theme) — raw hex is correct here per Roles v2 spec (roles have color + emoji).",
  "pborn": "Role color swatch / role chip: renders a USER-CHOSEN role color (data, not theme) — raw hex is correct here per Roles v2 spec (roles have color + emoji).",
  "S1vvou": "Role color swatch / role chip: renders a USER-CHOSEN role color (data, not theme) — raw hex is correct here per Roles v2 spec (roles have color + emoji).",
  "rPus5": "Role color swatch / role chip: renders a USER-CHOSEN role color (data, not theme) — raw hex is correct here per Roles v2 spec (roles have color + emoji).",
  "Z2OGTd": "Role color swatch / role chip: renders a USER-CHOSEN role color (data, not theme) — raw hex is correct here per Roles v2 spec (roles have color + emoji).",
  "MZMm9": "Role color swatch / role chip: renders a USER-CHOSEN role color (data, not theme) — raw hex is correct here per Roles v2 spec (roles have color + emoji).",
  "u9Yzf": "Role color swatch / role chip: renders a USER-CHOSEN role color (data, not theme) — raw hex is correct here per Roles v2 spec (roles have color + emoji).",
  "t7cNoQ": "Role color swatch / role chip: renders a USER-CHOSEN role color (data, not theme) — raw hex is correct here per Roles v2 spec (roles have color + emoji).",
  "vzm2c": "Role color swatch / role chip: renders a USER-CHOSEN role color (data, not theme) — raw hex is correct here per Roles v2 spec (roles have color + emoji).",
  "hdSkz": "Role color swatch / role chip: renders a USER-CHOSEN role color (data, not theme) — raw hex is correct here per Roles v2 spec (roles have color + emoji).",
  "StQk7": "Role color swatch / role chip: renders a USER-CHOSEN role color (data, not theme) — raw hex is correct here per Roles v2 spec (roles have color + emoji).",
  "Kg7b7": "Role color swatch / role chip: renders a USER-CHOSEN role color (data, not theme) — raw hex is correct here per Roles v2 spec (roles have color + emoji).",
  "j06hd": "Role color swatch / role chip: renders a USER-CHOSEN role color (data, not theme) — raw hex is correct here per Roles v2 spec (roles have color + emoji).",
  "LNL8w": "Role color swatch / role chip: renders a USER-CHOSEN role color (data, not theme) — raw hex is correct here per Roles v2 spec (roles have color + emoji).",
  "CKoMF": "Role color swatch / role chip: renders a USER-CHOSEN role color (data, not theme) — raw hex is correct here per Roles v2 spec (roles have color + emoji).",
  "v4XxJ": "Role color swatch / role chip: renders a USER-CHOSEN role color (data, not theme) — raw hex is correct here per Roles v2 spec (roles have color + emoji).",
  "UWRfa": "Role color swatch / role chip: renders a USER-CHOSEN role color (data, not theme) — raw hex is correct here per Roles v2 spec (roles have color + emoji).",
  "owU9F": "Role color swatch / role chip: renders a USER-CHOSEN role color (data, not theme) — raw hex is correct here per Roles v2 spec (roles have color + emoji).",
  "BGhmL": "Role color swatch / role chip: renders a USER-CHOSEN role color (data, not theme) — raw hex is correct here per Roles v2 spec (roles have color + emoji).",
  "d12O1": "Role color swatch / role chip: renders a USER-CHOSEN role color (data, not theme) — raw hex is correct here per Roles v2 spec (roles have color + emoji).",
  "VmKQU": "Role color swatch / role chip: renders a USER-CHOSEN role color (data, not theme) — raw hex is correct here per Roles v2 spec (roles have color + emoji).",
  "wvMmU": "Role color swatch / role chip: renders a USER-CHOSEN role color (data, not theme) — raw hex is correct here per Roles v2 spec (roles have color + emoji).",
  "SMnwM": "Role color swatch / role chip: renders a USER-CHOSEN role color (data, not theme) — raw hex is correct here per Roles v2 spec (roles have color + emoji).",
  "SHonj": "Role color swatch / role chip: renders a USER-CHOSEN role color (data, not theme) — raw hex is correct here per Roles v2 spec (roles have color + emoji).",
  "PaH2U": "Role color swatch / role chip: renders a USER-CHOSEN role color (data, not theme) — raw hex is correct here per Roles v2 spec (roles have color + emoji).",
  "Q5FgsL": "Role color swatch / role chip: renders a USER-CHOSEN role color (data, not theme) — raw hex is correct here per Roles v2 spec (roles have color + emoji).",
  "ZeduT": "Role color swatch / role chip: renders a USER-CHOSEN role color (data, not theme) — raw hex is correct here per Roles v2 spec (roles have color + emoji).",
  "v8j0v": "Role color swatch / role chip: renders a USER-CHOSEN role color (data, not theme) — raw hex is correct here per Roles v2 spec (roles have color + emoji).",
  "W1aszF": "Role color swatch / role chip: renders a USER-CHOSEN role color (data, not theme) — raw hex is correct here per Roles v2 spec (roles have color + emoji).",
  "w7TmgO": "Role color swatch / role chip: renders a USER-CHOSEN role color (data, not theme) — raw hex is correct here per Roles v2 spec (roles have color + emoji).",
  "QRgV6": "Role color swatch / role chip: renders a USER-CHOSEN role color (data, not theme) — raw hex is correct here per Roles v2 spec (roles have color + emoji).",
  "t62cN": "Role color swatch / role chip: renders a USER-CHOSEN role color (data, not theme) — raw hex is correct here per Roles v2 spec (roles have color + emoji).",
  "J3YTnS": "Role color swatch / role chip: renders a USER-CHOSEN role color (data, not theme) — raw hex is correct here per Roles v2 spec (roles have color + emoji).",
  "p4RR8": "Role color swatch / role chip: renders a USER-CHOSEN role color (data, not theme) — raw hex is correct here per Roles v2 spec (roles have color + emoji).",
  "VBcn5": "Role color swatch / role chip: renders a USER-CHOSEN role color (data, not theme) — raw hex is correct here per Roles v2 spec (roles have color + emoji).",
  "gGVcT": "Role color swatch / role chip: renders a USER-CHOSEN role color (data, not theme) — raw hex is correct here per Roles v2 spec (roles have color + emoji).",
  "okV6Z": "Role color swatch / role chip: renders a USER-CHOSEN role color (data, not theme) — raw hex is correct here per Roles v2 spec (roles have color + emoji).",
  "Rxu16": "Role color swatch / role chip: renders a USER-CHOSEN role color (data, not theme) — raw hex is correct here per Roles v2 spec (roles have color + emoji).",
  "bPeRJ": "Role color swatch / role chip: renders a USER-CHOSEN role color (data, not theme) — raw hex is correct here per Roles v2 spec (roles have color + emoji).",
  "DQH6R": "Switch knob: true-white knob on colored track is the standard control rendering in both themes.",
  "I1cewP": "Switch knob: true-white knob on colored track is the standard control rendering in both themes.",
  "zPi9A": "Switch knob: true-white knob on colored track is the standard control rendering in both themes.",
  "i7y22": "Switch knob: true-white knob on colored track is the standard control rendering in both themes.",
  "jVOYB": "Switch knob: true-white knob on colored track is the standard control rendering in both themes.",
  "DfJcy": "Switch knob: true-white knob on colored track is the standard control rendering in both themes.",
  "bDtmw": "2FA QR (desktop): true-white QR quiet zone required, same as QxyA1.",
  "CwVWw/WuSA5": "Camp Plot v2 mobile radial site-map (frame sq6hS, layout:none): this node is a transparent-fill (#00000000) outline graphic, so its bounding box is solid but its ink is only the stroke. Buitekring ring — 208x208 bbox, 1.3px circumference stroke. The BUITEKRING/BINNEKRING labels are hand-placed in the empty gaps BETWEEN arcs (label y=60-70 sits between arcs at y=44 and y=70); they never touch ink. Bbox-vs-ink false positive, not a render defect.",
  "CwVWw/h0egw": "Camp Plot v2 mobile radial site-map (frame sq6hS, layout:none): this node is a transparent-fill (#00000000) outline graphic, so its bounding box is solid but its ink is only the stroke. Construction ring — 156x156 bbox, 0.8px circumference stroke. Same bbox-vs-ink false positive as WuSA5; the BINNEKRING label sits in the clear gap above the Binnekring arc.",
  "CwVWw/coExp": "Camp Plot v2 mobile radial site-map (frame sq6hS, layout:none): this node is a transparent-fill (#00000000) outline graphic, so its bounding box is solid but its ink is only the stroke. Clock Streets — a full-canvas 328x300 path, so its bbox necessarily encloses EVERY label on the map (BUITEKRING, BINNEKRING, 1 KM, SCALE — APPROX). The radial street hairlines pass nowhere near those labels. Unavoidable bbox-vs-ink false positive for a map annotation layer.",
  "CwVWw/YeiVh": "Camp Plot v2 mobile radial site-map (frame sq6hS, layout:none): this node is a transparent-fill (#00000000) outline graphic, so its bounding box is solid but its ink is only the stroke. Main Avenues — full-canvas 328x300 path, identical situation to coExp. Scale-bar captions at y=280 sit well below all ring geometry and only intersect this path's bbox."
}
