# WSL download metadata
*Zone.Identifier

# Byte-identical duplicate of the root scope documents (verified by checksum)
AfrikaBurn App/

# Original scope PDFs stay out of main (one contains an unredactable example
# contact); preserved in full on the archive/source-documents branch
/AfricaBurn-*.pdf

# --- Node / monorepo build artefacts ---
node_modules/
.next/
.turbo/
dist/
out/
coverage/
*.tsbuildinfo

# --- Env files (all variants; .env.example is committed) ---
.env
.env.*
!.env.example
.vercel
.env*

# Agent worktrees — transient, never committed.
.claude/worktrees/

# Python build artefacts (design/qa/*.py tooling)
__pycache__/
*.pyc
