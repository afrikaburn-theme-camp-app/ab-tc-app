{
  "name": "quagga-portal-workspace",
  "private": true,
  "license": "FSL-1.1-ALv2",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/afrikaburn-theme-camp-app/ab-tc-app.git"
  },
  "homepage": "https://github.com/afrikaburn-theme-camp-app/ab-tc-app#readme",
  "bugs": {
    "url": "https://github.com/afrikaburn-theme-camp-app/ab-tc-app/issues"
  },
  "version": "0.0.0",
  "description": "AfrikaBurn Theme Camp App (Quagga Portal) \u2014 participant + org apps for theme-camp registration and entitlements",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "test:coverage": "turbo run test:coverage",
    "format": "prettier --write \"**/*.{ts,tsx,md,json}\"",
    "e2e:local": "./scripts/e2e-local.sh",
    "labels:sync": "tsx scripts/setup-github-labels.ts",
    "safe-chain:install": "./scripts/install-safe-chain.sh",
    "safe-chain:verify": "./scripts/verify-safe-chain.sh",
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.2.3",
    "@commitlint/config-conventional": "^21.2.3",
    "husky": "^9.1.7",
    "prettier": "^3.9.8",
    "tsx": "^4.23.15",
    "turbo": "^2.11.2",
    "typescript": "^6.0.3"
  },
  "packageManager": "pnpm@10.30.0",
  "engines": {
    "node": ">=22"
  },
  "pnpm": {
    "overrides": {
      "esbuild": ">=0.28.1"
    }
  }
}
