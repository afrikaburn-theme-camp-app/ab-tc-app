# Scripts

Store optional helper scripts for this skill.

Guidelines:
- Scripts should be idempotent when possible.
- Scripts should fail fast (`set -euo pipefail`).
- Scripts must not print secrets by default.
