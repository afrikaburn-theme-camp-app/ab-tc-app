# Minimal ownership for this repository. Expand only when a path truly needs
# a required reviewer — see GOVERNANCE.md for how ownership grows as the
# maintainer team grows.
#
# Patterns are last-match-wins. `/*` is root files only; subtrees need their
# own rules. CODEOWNERS only bites once branch protection requires code-owner
# review — deliberately not enabled yet; see GOVERNANCE.md for when it turns
# on (once the open-source migration PRs have landed).

# Base path — root-level files
/* @BeyersNel

# Essential admin / governance surfaces
/.github/ @BeyersNel
/LICENSE @BeyersNel
