# Minimal ownership for this fork. Expand only when a path truly needs a
# required reviewer — do not copy upstream’s production blast-radius map
# until this org actually owns that risk.
#
# Patterns are last-match-wins. `/*` is root files only; subtrees need their
# own rules. CODEOWNERS only bites once branch protection requires code-owner
# review (intentionally not enforced while setup is in progress).

# Base path — root-level files
/* @BeyersNel

# Essential admin / governance surfaces
/.github/ @BeyersNel
/LICENSE @BeyersNel
